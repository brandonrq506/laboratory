/** How long a notification badge stays visible (milliseconds) */
export const NOTIFICATION_DURATION = 5000;

/** How long the success state is shown after a successful action (milliseconds) */
export const SUCCESS_DURATION = 1000;

/** How long the error state is shown after a failed action (milliseconds) */
export const ERROR_DURATION = 2000;

/** Delay before triggering the open animation for HeadlessUI dialogs (milliseconds) */
export const MODAL_DURATION = 10;
